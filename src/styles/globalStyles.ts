import { StyleSheet } from 'react-native';

const globalStyles = StyleSheet.create({
    title: {
        fontSize: 25,
        marginBottom: 8,
        fontWeight: '700',
        color: '#222',
    },
    subtitle: {
        fontSize: 14,
        marginBottom: 8,
        fontWeight: '600',
        color: '#444',
    },
});

export default globalStyles;
